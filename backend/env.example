APP_ENV=dev
DEBUG=false
# With docker-compose: postgres on localhost:5432, churn DB auto-created
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/churn
ARTIFACTS_DIR=artifacts

# pgAdmin: http://localhost:5050 (admin@admin.com / admin)
# Connect to postgres: host=postgres (in Docker) or localhost (from host), port=5432
